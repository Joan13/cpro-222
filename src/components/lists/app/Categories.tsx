/**
 * Incomplete fragment in upstream repo — not imported elsewhere.
 * Replace with real implementation when available.
 */
import { View } from 'react-native';

export default function CategoriesPlaceholder() {
  return <View />;
}
